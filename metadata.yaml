name: astrbot_plugin_todoroom # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 清单自习室-Astr同步插件 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 与清单自习室APP待办同步，同时在本地以json格式保存数据。(需要token)
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: yuzu
repo: https://github.com/Hana-Yuzu/astrbot_plugin_todoroom
